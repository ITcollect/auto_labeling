# 基础依赖
python>=3.7.0
opencv-python>=4.5.0
PyQt5>=5.15.0
numpy>=1.20.0

# 自动化标注相关
torch>=1.8.0 ; sys_platform != 'darwin'  # Mac需单独安装
torchvision>=0.9.0
transformers>=4.12.0 ; extra == 'nlp'

# 数据格式处理
pycocotools>=2.0.0
lxml>=4.6.0  # Pascal VOC格式支持

# 开发工具（可选）
black>=21.0 ; extra == 'dev'
flake8>=3.9.0 ; extra == 'dev'

# 可选安装组
-e .[nlp]  # 自然语言处理扩展
-e .[dev]  # 开发工具
